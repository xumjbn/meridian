# ── 本地构建产物 / 运行时文件（勿提交）──────────────
/build/
/dist/

# 编译出的二进制
*.exe
backend/lynx-server
backend/lynx-server-*

# 加密主密钥（绝不可进版本库；泄露即等同泄露全部凭据）
.lynx_key
*.lynx_key

# 运行时数据库与日志
*.db
*.db-shm
*.db-wal
*.log

# 依赖（子目录另有 .gitignore，这里兜底）
node_modules/

# 系统/编辑器杂项
.DS_Store
Thumbs.db
.idea/
.vscode/
